# 📦 Инструкция установки обновлений

## Шаг 1: Установить новый пакет

```bash
# В папке проекта
npm install

# Или если используете yarn
yarn install
```

## Шаг 2: Настроить Email (ВАЖНО!)

### Для Gmail (рекомендуется):

1. **Включить 2-Factor Authentication:**
   - Перейти на https://myaccount.google.com/security
   - Включить 2FA (Two-Factor Authentication)

2. **Создать App Password:**
   - Перейти на https://myaccount.google.com/apppasswords
   - Выбрать App: Mail
   - Выбрать Device: Windows/Mac/Linux
   - Скопировать 16-символьный пароль

3. **Добавить в .env файл:**
   ```env
   SMTP_HOST=smtp.gmail.com
   SMTP_PORT=587
   SMTP_USER=your-email@gmail.com
   SMTP_PASS=your-16-char-app-password
   SMTP_FROM="ZEDLY Platform" <noreply@zedly.edu>
   APP_URL=http://localhost:5000
   ```

### Для других сервисов:

**Яндекс.Почта:**
```env
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=your-email@yandex.ru
SMTP_PASS=your-password
SMTP_FROM="ZEDLY Platform" <noreply@zedly.edu>
APP_URL=http://localhost:5000
```

**Mail.ru:**
```env
SMTP_HOST=smtp.mail.ru
SMTP_PORT=465
SMTP_USER=your-email@mail.ru
SMTP_PASS=your-password
SMTP_FROM="ZEDLY Platform" <noreply@zedly.edu>
APP_URL=http://localhost:5000
```

## Шаг 3: Запустить приложение

```bash
# Запуск разработки
npm run dev

# Или производство
npm start
```

## Шаг 4: Проверить работу

1. **Открыть админ панель**
   - Логин: `admin`
   - Пароль: `admin123`

2. **Добавить тестового пользователя**
   - Перейти на вкладку "Добавить пользователя"
   - Заполнить форму (обратите внимание - username уже НЕ требуется!)
   - Нажать "Добавить"
   - Должно появиться сообщение "✅ Email отправлен"

3. **Проверить email**
   - Перейти к почте на email, который указали
   - Найти письмо от ZEDLY Platform
   - Скопировать username и OTP пароль

4. **Протестировать вход**
   - Выйти из админ аккаунта
   - Использовать новый username и OTP пароль
   - При первом входе должно запросить сменить пароль

## 🔧 Структура username

### Ученики:
```
{класс}{секция}.{фамилия_транслит}.{первая_буква_имени}

Примеры:
- 9A.petrov.a
- 9A.sidorova.m
- 8B.ivanov.i
```

### Учителя:
```
T.{фамилия_транслит}.{первая_буква_имени}

Примеры:
- T.ivanova.m
- T.petrov.a
```

### Администраторы:
```
A.{фамилия_транслит}.{первая_буква_имени}

Примеры:
- A.sidorov.i
```

## ⚠️ Важные замечания

1. **Email теперь обязателен** - при создании пользователя Email поле больше не опционально
2. **Username генерируется автоматически** - администратору больше не нужно придумывать логины
3. **OTP отправляется по email** - пользователь получит пароль в почте
4. **Срок действия OTP - 5 часов** - если пользователь не вошел в течение этого времени, нужно создать нового пользователя

## 📊 Что изменилось в API

### POST /api/users/register

**Старый формат (ДО):**
```json
{
  "username": "student1",
  "role": "student",
  "firstName": "Алексей",
  "lastName": "Петров",
  "grade": "9",
  "gradeSection": "А",
  "email": "alexey@mail.com"
}
```

**Новый формат (ПОСЛЕ):**
```json
{
  "role": "student",
  "firstName": "Алексей",
  "lastName": "Петров",
  "grade": "9",
  "gradeSection": "А",
  "email": "alexey@mail.com"
}
```

**Ответ:**
```json
{
  "success": true,
  "data": {
    "_id": "1234567890",
    "username": "9A.petrov.a",
    "role": "student",
    "firstName": "Алексей",
    "lastName": "Петров",
    "email": "alexey@mail.com",
    "emailSent": true
  }
}
```

## 🐛 Отладка

### Проверить логи сервера:
```bash
# На сервере должны быть сообщения:
🔑 Generated username: 9A.petrov.a for Алексей Петров
✅ Email sent to alexey@mail.com
```

### Если email не отправляется:
1. Проверить SMTP параметры в .env
2. Убедиться, что сервис email доступен
3. Проверить логи ошибок сервера
4. В интерфейсе будет показан OTP для ручной передачи

## ✅ Checklist после установки

- [ ] Установлены все пакеты (`npm install`)
- [ ] Настроены SMTP параметры в .env
- [ ] Приложение запущено без ошибок
- [ ] Админ панель доступна
- [ ] Тестовый пользователь создан успешно
- [ ] Email отправлен и получен
- [ ] Вход с новым username и OTP работает
- [ ] Смена пароля при первом входе работает

## 📞 Помощь

Если что-то не работает:

1. **Проверить консоль браузера** (F12) - есть ли ошибки JavaScript
2. **Проверить логи сервера** - как проходит создание пользователя
3. **Проверить .env файл** - все ли параметры заполнены
4. **Перезагрузить приложение** - иногда помогает
5. **Проверить email конфигурацию** - доступен ли SMTP сервер

---

Готово! 🎉 Система полностью обновлена и готова к использованию!
